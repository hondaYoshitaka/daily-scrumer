/*
 *  mdl.keep_in_mind.jsのテスト
 *
 */

var should = require('should'),
    db = require('../../db'),
    KeepInMind = db.models['KeepInMind'];

describe('mdl.keep_in_mind', function(){
    before(function(done){

    });
    it('findByTeamSprint', function(done){

        done();
    });
});